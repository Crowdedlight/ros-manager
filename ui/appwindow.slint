
import { MenuBar } from "menu.slint";
import { MainContentList } from "listbox.slint";

MainWindow := Window {
    title: "ROS Manager";
    default-font-family: "Noto Sans";
    HorizontalLayout {
        padding: 0; spacing: 0;
        MenuBar {}
        MainContentList {}
    }
}

